services:
  my-cloud:
    build: .
    container_name: my-cloud
    ports:
      - "8686:8686"
    environment:
      # File ownership for created files (defaults are Unraid's nobody:users).
      - PUID=99
      - PGID=100
      - UMASK=022
    volumes:
      # App settings, users, shares (tiny JSON files)
      - ./config:/config
      # Your files live here: /data/users/<username>/files
      - ./data:/data
    restart: unless-stopped
